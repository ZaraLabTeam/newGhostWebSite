{{!< default}}

{{! The comment above "< default" means - insert everything in this file into
    the {body} of the default.hbs template, which contains our header/footer. }}

<section class="content animated fadeInDownBig" role="main">
  {{! Everything inside the #post tags pulls data from the post }}
  {{#post}}
  <article class="{{post_class}} main-article">
    <div class="inner">
      {{! Everything below outputs content of the the post which has been published }}
      <time class="time" datetime="{{date format='DD MM YYYY'}}">{{date format="DD/MM/YYYY"}}</time>
      <h1 class="post-title">{{{title}}}</h1>
      {{#if image}}<img src={{image}} alt="{{{title}}}"/>{{/if}}
      {{content}}
      {{#if tags}}<p class="post-tags">{{tags separator=", "}}</p>{{/if}}
      {{> share}}
    </div>
  </article>
  {{> disqus}}
  {{/post}}
</section>
