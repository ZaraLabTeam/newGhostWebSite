{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

<main class="home-page">

	{{! The big featured header }}
	{{> logo}}
	<!-- <section class="carousel">
		{{!> "main-logo" }}
		{{!> "carousel" }}
	</section > -->

	<div class="homepage-container">

		{{! The big featured who we are section }}
		<section class="homepage-row who-we-are">
			{{> "who-we-are"}}
		</section>

		{{! The big featured coworking}}
		<section class="coworking">
			{{> "pricing"}}
		</section>

		{{! The big featured last events}}
		<section class="homepage-row last-events">
			{{> "last-events"}}
		</section>

		{{! The big featured stream from youtube}}
		<section class="homepage-row player">
			{{> "stream-player"}}
		</section>

		{{! The big featured youtube}}
		<section class="homepage-row last-youtube-videos">
			{{> "last-youtube-videos"}}
		</section>

		{{! The big featured news}}
		<section class="homepage-row news">
			{{> "last-news"}}
		</section>

		{{! The big featured sponsors}}
		 <section class="homepage-row home-sponsors">
			{{> "home-sponsors"}}
		</section>
	</div>

	<section class="video-modal">
		{{> video-modal}}
	</section>
</main>
